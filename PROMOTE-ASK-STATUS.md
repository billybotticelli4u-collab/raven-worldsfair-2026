# Promote ask status

**WITHDRAWN** prior ask for `fa205f85` alone.

Tip carries: apex evidence restore (apex `index.html` / `index.template.html` / `.vercelignore` byte-identical to `main` 8336f86d; Fair footer link dropped, `/worldsfair` reachable via the allowlisted `vercel.json` redirect) + `apps/package.json` CommonJS scope (outside apex) + D5/D6 harness with real-git-checkout LOCAL-UNBOUND (loopback only) + download note renamed to `DOWNLOAD-NOTES.md` with `git check-ignore` deployment-input proof + OWNER-PROMOTE-STEP preview-not-merge rule.

Site suite on this HEAD: 139 PASS / exactly the 5 documented lineage ENOENT failures / 0 real tip defects.

**Promote means:** promote the branch's Vercel preview deployment — **never** merge into `main`.

New one-line YES/NO ask opens only after CODEX delta re-review PASS on this HEAD. This successor was authored by Claude desktop as Billy's stand-in; Claude is disqualified from reviewing it.

No deploy. No push.
Assembled: 2026-09-20
