> Local draft for independent review. Author: desktop CODEX. Source: Fair candidate `7c61124c54c61537c5240be3bc88c60855201234`, tree `1bc9409820ef9128798c2d7e6940b8b840b4644b`. These documents do not establish what is currently deployed. Independent reviewer: GROK; corrections await re-review. The author cannot self-approve.

# Judge surface: threat model and current limits

Protect the local host, report integrity and the reader's understanding of what was actually measured. Inputs include the selected curated target, HTTP request bodies, target output, report identifiers and replay paths. The target process and browser display are separate boundaries; implementation outcomes must not become evidence of more than the declared corpus tests.

The current app documents bounded run time, output caps, an environment allowlist, ephemeral work directories and process-group cleanup. Reports disclose the isolation mode. Darwin Seatbelt and Linux Node permissions have different capabilities. Node permissions are not an OS sandbox and do not restrict network access. A child process alone is not a sandbox. Host-specific evidence must not be generalized to an untested deployment.

The HTTP surface documents a 64 KiB body limit, five seconds to finish a body, target timeouts from 100 to 10000 ms, constrained report IDs, no overwrite of existing reports and one in-process execution lock. HTTP replay is limited to regular JSON files directly under the app's reports or examples directories; CLI replay has a different path boundary. These controls do not establish rate limiting, authentication, cross-instance locking or safe multi-tenant execution.

The identity gate blocks normal production routes when identity is UNKNOWN; health and build-info remain available. In production (`VERCEL=1` or `NODE_ENV=production`), UNKNOWN identity blocks the home route with JSON HTTP 503 (`identity_unavailable`), not the visual badge. Default local `npm start` without those production flags still serves HTML for UNKNOWN identity, including the badge. Operator-only commit claims are displayed as unverified and are not selected. CONFLICT remains a serving state with a warning; generated assertions can also serve. Therefore this gate is not proof of git-only deployment authority. Build metadata is an assertion; the public-file fingerprint covers three UI files, not the backend or the deployment chain.

Residual risks include misuse of local-only assumptions in public hosting, stale or substituted delivery instructions, broader credentials than an agent requires, and interpreting an infrastructure failure as proof a permission restriction worked. I-1 and I-3 remain CHANGES_REQUESTED on their own packets. I-3's reviewed source is not `7c61124c`. Do not read those packets as this candidate. Do not infer production readiness from this document or from component review verdicts.
